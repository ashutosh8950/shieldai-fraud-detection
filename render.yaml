services:
  - type: web
    name: shieldai-backend
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: python train_model.py && python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0